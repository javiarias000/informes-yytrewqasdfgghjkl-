node_modules/
.env
token.json
credentials.json
/tmp/
*.log

# Datos de estudiantes (sensibles — no versionar)
Datos/*.csv
Datos/*.xlsx
Datos/*.xls

# Historial de envíos y overrides de docentes (datos sensibles)
submissions.json
docentes_overrides.json

# Base de datos SQLite (datos personales — regenerar con seed-db.js)
conservatorio.db
